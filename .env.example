# Leave ANTHROPIC_API_KEY unset to run the planner in deterministic mock mode
# (the whole pipeline + demo still work offline).
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-5
MAX_TOKENS=2000
DEFAULT_ADAPTER=opentrons
